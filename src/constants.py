c1 = 3  # tuning constant for contender attribute calculation
c2 = 6  # paper says should be bigger then 2, tuning constant as well
super_graph_depth = 2  # super-graph depth, for now probably 2 is enough, not generic for else
tu = 5  # random walk length
